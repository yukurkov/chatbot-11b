version: 1
project: my-project

repositories:

    echobot:

        url: https://github.com/yukurkov/chatbot_11b.git
        branch: main

        # Set CPU and Memory for first container startup

        cpu: 1
        cpuFraction: 20
        memory: 256MiB
